# 🛍️ Next.js + Sanity CMS Интернет-магазин

Современный интернет-магазин с минималистичным дизайном и яркими акцентами. Полнофункциональное решение для e-commerce с мощной админкой для управления товарами.

## ✨ Особенности

- 🎨 **Минималистичный дизайн** с яркими акцентами (основной цвет: #7C3AED, акцент: #EC4899)
- 📱 **Полностью адаптивный** - идеально работает на всех устройствах
- 🛒 **Функциональная корзина** с сохранением состояния (Zustand)
- ⚡ **Молниеносная загрузка** - Server Side Rendering и Static Generation
- 🔍 **SEO оптимизация** - готов к индексации поисковиками
- 🎛️ **Мощная админка** - Sanity Studio для управления контентом
- 🖼️ **Оптимизация изображений** - автоматическое сжатие и lazy loading
- 🔄 **Real-time превью** - видите изменения мгновенно

## 🚀 Технологии

- **Next.js 14** - React фреймворк с App Router
- **Sanity CMS v3** - Headless CMS для управления контентом
- **TypeScript** - Строгая типизация для надёжности кода
- **Tailwind CSS** - Utility-first CSS фреймворк
- **Zustand** - Лёгкое управление состоянием
- **Heroicons** - Красивые SVG иконки
- **HeadlessUI** - Доступные UI компоненты

## 📦 Установка и запуск

### Шаг 1: Клонирование и установка зависимостей

```bash
# Установите зависимости для фронтенда
npm install

# Установите зависимости для Sanity
cd sanity
npm install
cd ..
```

### Шаг 2: Настройка Sanity

1. Зарегистрируйтесь на [sanity.io](https://www.sanity.io/) (бесплатно)
2. Создайте новый проект в Sanity Dashboard
3. Получите **Project ID** и **Dataset** из настроек проекта

### Шаг 3: Переменные окружения

Создайте файл `.env.local` в корне проекта:

```env
NEXT_PUBLIC_SANITY_PROJECT_ID=ваш_project_id
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01
```

Обновите файл `sanity/sanity.config.ts` с вашим Project ID и Dataset.

### Шаг 4: Первый запуск

Запустите оба сервера одновременно:

**Terminal 1 - Next.js (фронтенд):**
```bash
npm run dev
```
Откройте http://localhost:3000

**Terminal 2 - Sanity Studio (админка):**
```bash
npm run sanity:dev
```
Откройте http://localhost:3333 или перейдите на http://localhost:3000/studio

### Шаг 5: Добавление контента

1. Откройте Sanity Studio (http://localhost:3333)
2. Авторизуйтесь через Google/GitHub
3. Создайте несколько категорий
4. Добавьте товары с изображениями
5. Опуб## 📁 Структура проекта

```
├── app/                        # Next.js App Router
│   ├── page.tsx               # Главная страница
│   ├── layout.tsx             # Общий layout
│   ├── globals.css            # Глобальные стили
│   ├── catalog/               # Каталог товаров
│   │   └── page.tsx          # Страница каталога с фильтрами
│   ├── products/[slug]/       # Динамический роутинг товаров
│   │   └── page.tsx          # Страница отдельного товара
│   ├── checkout/              # Оформление заказа
│   │   └── page.tsx          
│   ├── about/                 # Страница О нас
│   │   └── page.tsx          
│   ├── studio/[[...index]]/   # Sanity Studio (админка)
│   │   └── page.tsx          
│   └── not-found.tsx          # Кастомная страница 404
│
├── components/                 # React компоненты
│   ├── layout/                # Layout компоненты
│   │   ├── Header.tsx        # Шапка с навигацией
│   │   └── Footer.tsx        # Подвал
│   ├── catalog/               # Компоненты каталога
│   │   ├── ProductCard.tsx   # Карточка товара
│   │   ├── ProductGrid.tsx   # Сетка товаров
│   │   ├── CategoryFilter.tsx # Фильтр по категориям
│   │   └── AddToCartButton.tsx # Кнопка добавления в корзину
│   └── cart/                  # Компоненты корзины
│       └── CartDrawer.tsx    # Выдвижная панель корзины
│
├── lib/                        # Утилиты и конфигурация
│   ├── sanity/                # Sanity интеграция
│   │   ├── client.ts         # Sanity клиент
│   │   └── queries.ts        # GROQ запросы
│   └── store/                 # State management
│       └── cart.ts           # Zustand store для корзины
│
├── sanity/                     # Sanity Studio
│   ├── sanity.config.ts       # Конфигурация Sanity
│   ├── sanity.cli.ts          # CLI конфигурация
│   └── schemas/               # Схемы данных
│       ├── index.ts          # Экспорт всех схем
│       ├── product.ts        # Схема товара
│       ├── category.ts       # Схема категории
│       └── settings.ts       # Схема настроек
│
├── types/                      # TypeScript типы
│   └── index.ts               # Общие типы проекта
│
├── .env.local.example          # Пример переменных окружения
├── next.config.js              # Конфигурация Next.js
├── tailwind.config.ts          # Конфигурация Tailwind
└── package.json                # Зависимости проекта
```

## 🎨 Дизайн-система

### Цветовая палитра

- **Primary (Фиолетовый)**: `#7C3AED` - основной цвет для CTA и акцентов
- **Accent (Розовый)**: `#EC4899` - дополнительный акцент
- **Нейтральные**: оттенки серого для текста и фонов

### Компоненты

Все компоненты следуют единому стилю:
- Закруглённые углы (`rounded-2xl`, `rounded-lg`)
- Мягкие тени (`shadow-sm`, `shadow-md`)
- Плавные переходы (`transition-all`, `duration-300`)
- Консистентные отступы (`gap-4`, `gap-6`)

## 🛠️ Команды

```bash
# Разработка
npm run dev              # Запуск Next.js dev сервера
npm run sanity:dev       # Запуск Sanity Studio

# Продакшн
npm run build            # Сборка Next.js
npm start                # Запуск продакшн сервера
npm run sanity:build     # Сборка Sanity Studio
npm run sanity:deploy    # Деплой Sanity Studio

# Линтинг
npm run lint             # Проверка кода ESLint
```

## 📝 Работа с контентом (Sanity Studio)

### Схемы данных

**Product (Товар)**
- Название, описание
- Цена и цена для сравнения
- Изображения (основное + галерея)
- Категория
- Наличие на складе
- Избранный товар (featured)

**Category (Категория)**
- Название
- Slug
- Описание

**Settings (Настройки)**
- Название магазина
- Описание
- Логотип
- Контактная информация

## 🚢 Деплой

### Vercel (рекомендуется)

1. Push код в GitHub
2. Импортируйте проект в [Vercel](https://vercel.com)
3. Добавьте переменные окружения
4. Деплой автоматический!

### Sanity Studio

```bash
npm run sanity:deploy
```

Получите URL вашей админки (например, `yourproject.sanity.studio`)

## 🔧 Кастомизация

### Изменение цветов

Отредактируйте `tailwind.config.ts`:

```typescript
colors: {
  primary: {
    DEFAULT: '#ВАШ_ЦВЕТ',
    // ...
  },
}
```

### Добавление новых полей в товары

1. Откройте `sanity/schemas/product.ts`
2. Добавьте новое поле в массив `fields`
3. Обновите тип в `types/index.ts`
4. Обновите компоненты для отображения

## 🐛 Troubleshooting

**Sanity не подключается?**
- Проверьте `.env.local` файл
- Убедитесь, что Project ID правильный
- Проверьте CORS настройки в Sanity Dashboard

**Изображения не загружаются?**
- Добавьте домен Sanity CDN в `next.config.js` → `images.domains`

**TypeScript ошибки?**
- Запустите `npm run build` для проверки
- Убедитесь, что все типы импортированы

## 📄 Лицензия

MIT - свободно используйте для своих проектов!

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы, создайте Issue в репозитории.

---

Сделано с ❤️ и Next.jsи

## Лицензия

MIT
